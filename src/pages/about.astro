---
/**
 * About page
 */
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="About" currentPath="/about/">
  <article class="about-page">
    <header class="page-header">
      <h1 class="page-title">About</h1>
    </header>

    <div class="about-content">
      <p>
        Maker, breaker, rustacean 🦀, devops.
      </p>

      <p>
        Here you'll find stories, projects and most importantly, personal opinions and thoughts.
      </p>

      <p>
        I'm <strong>Dimitris Zervas</strong> and this is my blog. While the blog code is mostly
        AI generated, the content is entirely my own - hand crafted bits and bytes.
      </p>

      <p>
        For questions/ideas/whatever, hit me up at <a href="mailto:dzervas@dzervas.gr">dzervas at dzervas dot gr</a>.
      </p>

      <p class="closing">Have a nice trip and happy hacking! :)</p>
    </div>
  </article>
</BaseLayout>

<style>
  .about-page {
    max-width: 100%;
    margin: 0 auto;
  }

  .page-header {
    margin-bottom: 2.5rem;
  }


  .page-title {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--color-text);
    margin: 0;
  }

  .about-content {
    font-size: 1.125rem;
    line-height: 1.75;
    color: var(--color-text-secondary);
  }

  .about-content p {
    margin-bottom: 1.5rem;
  }

  .about-content strong {
    color: var(--color-text);
    font-weight: 600;
  }

  .about-content a {
    color: var(--color-accent);
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .about-content a:hover {
    text-decoration-thickness: 2px;
  }

  .warning-text {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    padding: 1rem 1.25rem;
    background: var(--color-callout-warning-bg);
    border-left: 3px solid var(--color-callout-warning-border);
    border-radius: 0 6px 6px 0;
    color: var(--color-callout-warning-text);
  }

  .warning-text :global(.icon) {
    flex-shrink: 0;
    margin-top: 0.125rem;
    color: var(--color-callout-warning-title);
  }

  .closing {
    font-style: italic;
    color: var(--color-text-muted);
  }
</style>
